{
  "name": "blackridge-native-frame-codec-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "demo": "npm run build:native && tsx src/demo.ts",
    "benchmark": "npm run build:native && tsx src/benchmark.ts",
    "test": "npm run build:native && vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm test && npm run demo"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
